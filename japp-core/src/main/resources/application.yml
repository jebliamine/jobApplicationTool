spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/japp
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  app:
    bootstrap-admin:
      email: admin@japp.com
      password: ChangeMe123!

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

logging:
  level:
    org.springframework.security: TRACE